class Solution(object):
    def removeDuplicates(self, nums):
        i=0
        while i<(len(nums)):
            count=nums.count(nums[i])
            if count>2:
                del (nums[i:i+count-2])
            else:
                i+=1
        return len(nums)
