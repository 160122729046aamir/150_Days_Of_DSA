class Solution(object):
    def minOperations(self, logs):
        x = 0
        for i in range(len(logs)):
            if logs[i] == './':
                x += 0
            elif logs[i] == '../':
                if x > 0:
                    x -= 1
            else:
                x += 1
        return x
