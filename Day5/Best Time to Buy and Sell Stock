class Solution(object):
    def maxProfit(self, prices):
        if not prices:
            return 0
        min_price=prices[0]
        max_profit=0
        for x in prices:
            if x<min_price:
                min_price=x
            elif x-min_price>max_profit:
                max_profit = x - min_price
        return max_profit

