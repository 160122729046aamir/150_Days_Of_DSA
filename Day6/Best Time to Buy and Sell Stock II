class Solution(object):
    def maxProfit(self, prices):
        profit=0
        a=prices[:]
        for i in range(1,len(prices)):
            if a[i]>a[i-1]:
                profit+=a[i]-a[i-1]
        return profit
