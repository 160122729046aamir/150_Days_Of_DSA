def check_pair(arr,sum):
    for i in range(len(arr)):
        for j in range(i+1,len(arr)):
            if arr[i]+arr[j]==sum:
                return True
    return False
print(check_pair([11, 15, 26, 38, 9, 10],45))
