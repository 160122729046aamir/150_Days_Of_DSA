class Solution(object):
    def numWaterBottles(self, numBottles, numExchange):
        e=numBottles
        d=numBottles
        
        while e>=numExchange:
            newbottles=e//numExchange
            e=newbottles+e%numExchange
            d=d+newbottles
        return d
