from collections import defaultdict
class Solution(object):
    def groupAnagrams(self, strs):
        hashmap=defaultdict(list)
        for word in strs:
            sorted_tuple=tuple(sorted(word))
            hashmap[sorted_tuple].append(word)
        return list(hashmap.values())
              
